import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import { decorators } from '../../storybook/decorators';

import { ImageUploader } from '.';

<Meta
  title="Components/ImageUploader"
  component={ImageUploader}
  decorators={decorators}
  args={{
    uploadURL: 'http://localhost:4000/v1/uploads/images',
    onSuccess: (file) => console.log({ file }),
    onError: () => console.log('Upload image failed!'),
    locale: {
      dropPasteFiles: 'Adicione seu image abaixo',
      back: 'Voltar',
      save: 'Salvar',
      cancel: 'Cancelar',
      pauseUpload: 'Pausar carregamento',
      retryUpload: 'Tente carregar novamente',
      resumeUpload: 'Continuar o carregamento',
      uploadPaused: 'Upload pausado',
      uploadComplete: 'Upload completo',
      myDevice: 'Meu dispositivo'
    }
  }}
/>

export const Template = (args) => (
  <ImageUploader {...args}>
    <Text>Clique aqui para adicionar o seu image</Text>
  </ImageUploader>
);

# ImageUploader

<Canvas>
  <Story name="Upload" args={null}>
    {Template.bind({})}
  </Story>
</Canvas>

# Props

<ArgsTable of={ImageUploader} />
