import { Meta, Story } from '@storybook/addon-docs';
import { Box } from '../box';
import { Card, CardBody } from '../card';
import { Stack } from '../stack';
import { Icon } from '../icon';
import { Title } from '../typography';
import { decorators } from '../../storybook/decorators';
import { colorNames } from '../../styles/themes/colors';

import { Button } from '.';

<Meta title="Components/Button" component={Button} decorators={decorators} />

export const Template = (args) => (
  <>
    <Stack direction="column" spacing="5">
      {colorNames.map((color) => (
        <Stack direction="column" spacing="5">
          <Title fontSize="3xl">{color.toUpperCase()}</Title>
          <Card background={color == 'white' ? 'black.200' : 'white.500'}>
            <CardBody>
              <Stack direction="row" spacing="5">
                <Button {...args} colorScheme={color}>
                  Text
                </Button>
                <Button
                  {...args}
                  leftIcon={<Icon name="play" />}
                  colorScheme={color}
                >
                  Play
                </Button>
                <Button
                  {...args}
                  rightIcon={<Icon name="stop" />}
                  colorScheme={color}
                >
                  Stop
                </Button>
                <Button {...args} isLoading colorScheme={color}>
                  Loading
                </Button>
                <Button {...args} variant="outline" colorScheme={color}>
                  Outline
                </Button>
                <Button {...args} variant="ghost" colorScheme={color}>
                  Ghost
                </Button>
                <Button {...args} variant="link" colorScheme={color}>
                  Link
                </Button>
              </Stack>
            </CardBody>
          </Card>
        </Stack>
      ))}
    </Stack>
  </>
);

<Story name="Colors" args={null}>
  {Template.bind({})}
</Story>
