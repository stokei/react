import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import { Stack } from '../stack';
import { Title } from '../title';
import { decorators } from '../../storybook/decorators';
import { useUploadFiles } from '../../hooks/use-upload-files';

import { InputFileListItem } from '.';

<Meta
  title="Components/InputFileListItem"
  component={InputFileListItem}
  decorators={decorators}
/>

export const Template = (args) => {
  return (
    <InputFileListItem
      fileURL="https://google.com/my-file.png"
      filename="my-file.png"
      size={523333}
      {...args}
    />
  );
};

# Initial state

<Canvas>
  <Story name="Initial state">{Template.bind({})}</Story>
</Canvas>

# Upload progress

<Canvas>
  <Story
    name="Upload progress"
    args={{
      progress: 58
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

# Error

<Canvas>
  <Story
    name="Error"
    args={{
      isFinished: true,
      success: false
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

# Success

<Canvas>
  <Story
    name="Success"
    args={{
      isFinished: true,
      success: true
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

# Props

<ArgsTable of={InputFileListItem} />
