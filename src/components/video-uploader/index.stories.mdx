import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import { decorators } from '../../storybook/decorators';

import { VideoUploader } from '.';

<Meta
  title="Components/VideoUploader"
  component={VideoUploader}
  decorators={decorators}
  args={{
    uploadURL: 'http://localhost:4000/v1/uploads/videos',
    onSuccess: (file) => console.log({ file }),
    onError: () => console.log('Upload video failed!'),
    locale: {
      dropPasteFiles: 'Adicione seu video abaixo',
      back: 'Voltar',
      save: 'Salvar',
      cancel: 'Cancelar',
      pauseUpload: 'Pausar carregamento',
      retryUpload: 'Tente carregar novamente',
      resumeUpload: 'Continuar o carregamento',
      uploadPaused: 'Upload pausado',
      uploadComplete: 'Upload completo',
      myDevice: 'Meu dispositivo'
    }
  }}
/>

export const Template = (args) => (
  <VideoUploader {...args}>
    <Text>Clique aqui para adicionar o seu video</Text>
  </VideoUploader>
);

# VideoUploader

<Canvas>
  <Story name="Upload" args={null}>
    {Template.bind({})}
  </Story>
</Canvas>

# Props

<ArgsTable of={VideoUploader} />
