import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import { decorators } from '../../storybook/decorators';

import { VideoUploader } from '.';

<Meta
  title="Components/VideoUploader"
  component={VideoUploader}
  decorators={decorators}
  args={{
    accountId: 'acc_stokei',
    appId: 'app_stokei',
    uploadURL: 'http://localhost:4000/v1/uploads/file_clba3xtd50002m26k5pj5oeky'
  }}
/>

export const Template = (args) => (
  <VideoUploader {...args}>
    <Text>Clique aqui para adicionar o seu video</Text>
  </VideoUploader>
);

# VideoUploader

<Canvas>
  <Story name="Upload" args={null}>
    {Template.bind({})}
  </Story>
</Canvas>

# Props

<ArgsTable of={VideoUploader} />
